# Release — Kooki

Antes de publicar: executar lint, typecheck, testes e build; aplicar migration em staging; testar Auth/RLS/cache/storage; configurar OAuth e secrets; validar importação real e exclusão de conta. Rollback: restaurar deploy anterior e migration reversa revisada; não aplicar downgrade destrutivo automático.
