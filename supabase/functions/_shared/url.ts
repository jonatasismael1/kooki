const blockedHosts=new Set(['localhost','0.0.0.0','127.0.0.1','::1','169.254.169.254','metadata.google.internal'])
const privateIp=/^(10\.|127\.|169\.254\.|192\.168\.|172\.(1[6-9]|2\d|3[01])\.)/
export function safeUrl(raw:string){const u=new URL(raw);if(!['http:','https:'].includes(u.protocol))throw new Error('Protocolo não permitido');if(blockedHosts.has(u.hostname)||privateIp.test(u.hostname)||u.hostname.endsWith('.local')||u.hostname.endsWith('.internal'))throw new Error('Destino privado bloqueado');u.hash='';for(const k of [...u.searchParams.keys()])if(k.startsWith('utm_')||['fbclid','gclid'].includes(k))u.searchParams.delete(k);return u}
export async function fetchLimited(url:URL){const controller=new AbortController();const timeout=setTimeout(()=>controller.abort(),8000);try{const response=await fetch(url,{signal:controller.signal,redirect:'error',headers:{'User-Agent':'KookiRecipeBot/1.0'}});if(!response.ok)throw new Error(`Fonte respondeu ${response.status}`);const length=Number(response.headers.get('content-length')??0);if(length>2_000_000)throw new Error('Conteúdo excede 2 MB');const text=await response.text();if(text.length>2_000_000)throw new Error('Conteúdo excede 2 MB');return text}finally{clearTimeout(timeout)}}
